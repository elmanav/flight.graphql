schema {
  query: Query
}

type Query {
  aircrafts: [Aircraft!]!
  aircraft(regNumber: String!): Aircraft
  aircraftByAny(value: ValueInput!): Aircraft!
}

type Aircraft {
  id: Int!
  regNumber: String!
  iCAOCode: String!
}

input ValueInput {
  typeName: String!
  value: Any!
}

scalar Any